---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - esbuild.js
  - .vscode-test.mjs
  - .vscodeignore
  - .vscode/launch.json
  - media/icon.svg
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm install completes without errors"
    - "esbuild.js script exists with correct VS Code extension configuration"
    - "tsconfig.json targets ES2024 with strict mode and outDir set to out (not dist)"
    - "package.json declares all required devDependencies and npm scripts"
    - ".vscode-test.mjs configures @vscode/test-cli to find tests in out/test/"
  artifacts:
    - path: "package.json"
      provides: "Extension manifest with contributes, scripts, and devDependencies"
      contains: "logautopsy.runAnalysis"
    - path: "tsconfig.json"
      provides: "TypeScript config for type checking (tsc --noEmit) and test compilation"
      contains: "strict"
    - path: "esbuild.js"
      provides: "Build script with watch mode and production flag"
      contains: "external"
    - path: ".vscode-test.mjs"
      provides: "Test runner configuration"
      contains: "defineConfig"
    - path: ".vscode/launch.json"
      provides: "F5 Extension Development Host launch config"
      contains: "extensionHost"
    - path: "media/icon.svg"
      provides: "Placeholder activity bar icon"
      contains: "svg"
  key_links:
    - from: "package.json"
      to: "esbuild.js"
      via: "npm scripts (compile, watch, package)"
      pattern: "node esbuild.js"
    - from: "package.json"
      to: ".vscode-test.mjs"
      via: "npm test -> vscode-test CLI"
      pattern: "vscode-test"
    - from: "tsconfig.json"
      to: "package.json"
      via: "check-types script"
      pattern: "tsc --noEmit"
---

<objective>
Create the complete build infrastructure for the LogAutopsy VS Code extension: package.json manifest with all contribution points, esbuild build script, TypeScript configuration, test runner configuration, and VS Code launch configuration.

Purpose: Establish the toolchain foundation that all source code (Plan 02) and all subsequent phases depend on. Without this, nothing compiles, bundles, or tests.
Output: A project root with all config files, node_modules installed, and npm scripts ready to run (though compilation will fail until Plan 02 adds source files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-RESEARCH.md
@.planning/phases/01-scaffold/01-PATTERNS.md
@.planning/phases/01-scaffold/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all project configuration files</name>
  <files>package.json, tsconfig.json, esbuild.js, .vscode-test.mjs, .vscodeignore, .vscode/launch.json, media/icon.svg</files>
  <read_first>
    .planning/phases/01-scaffold/01-PATTERNS.md
    .planning/phases/01-scaffold/01-RESEARCH.md
    .planning/phases/01-scaffold/01-CONTEXT.md
  </read_first>
  <action>
Create all configuration files using the exact patterns from PATTERNS.md. Each file's content is fully specified there. Key details:

**package.json** — Full manifest per PATTERNS.md `package.json` section:
- `"name": "logautopsy"`, `"displayName": "LogAutopsy"`, `"version": "0.1.0"`, `"publisher": "logautopsy-dev"`
- `"engines": { "vscode": "^1.90.0" }`
- `"activationEvents": []` — empty array, NOT omitted (per D-06, VS Code 1.74+ infers from contributes)
- `"main": "./dist/extension.js"` — esbuild output path, NOT `./out/extension.js` (per D-08)
- `contributes.commands`: one command `logautopsy.runAnalysis` with title "Run Analysis" and category "LogAutopsy"
- `contributes.viewsContainers.activitybar`: one container `logautopsy-container` with title "LogAutopsy" and icon `media/icon.svg` (per D-07)
- `contributes.views.logautopsy-container`: one view `logautopsy.sidebar` with name "Analysis Results" (per D-07)
- Scripts: `vscode:prepublish`, `compile`, `watch`, `watch:esbuild`, `watch:tsc`, `package`, `check-types`, `pretest`, `test` — exact values in PATTERNS.md
- devDependencies: `@types/mocha` ^10.0.10, `@types/node` ^25.6.0, `@types/vscode` ^1.90.0, `@vscode/test-cli` ^0.0.9, `@vscode/vsce` ^3, `esbuild` ^0.21, `mocha` ^10, `npm-run-all` ^4.1.5, `typescript` ~5.4

**tsconfig.json** — Per PATTERNS.md:
- `"module": "commonjs"`, `"target": "ES2024"`, `"lib": ["ES2024"]`
- `"outDir": "out"` — NOT `dist` (tsc output for tests goes to out/, esbuild writes to dist/)
- `"sourceMap": true`, `"strict": true`
- `"rootDir": "src"`
- `"include": ["src/**/*.ts", "test/**/*.ts"]`
- `"exclude": ["node_modules", ".vscode-test"]`

**esbuild.js** — Per PATTERNS.md, full Node.js build script:
- `require('esbuild')` (CommonJS)
- Entry: `src/extension/activate.ts` (per D-08)
- `bundle: true`, `format: 'cjs'`, `platform: 'node'`, `outfile: 'dist/extension.js'`
- `external: ['vscode']` — mandatory, vscode is a host-provided virtual module
- `sourcemap: !production` (per D-10)
- `minify: production`
- Watch mode via `esbuild.context().watch()`
- esbuild-problem-matcher plugin for VS Code problem matcher integration

**.vscode-test.mjs** — Per PATTERNS.md:
- ESM format (`.mjs` extension)
- `import { defineConfig } from '@vscode/test-cli'`
- `files: 'out/test/**/*.test.js'` — targets tsc-compiled test output
- `extensionDevelopmentPath: '.'`
- `mocha: { timeout: 20000 }`

**.vscodeignore** — Per PATTERNS.md, exclude non-essential files from VSIX:
```
.vscode-test/**
src/**
test/**
out/**
.vscode-test.mjs
esbuild.js
tsconfig.json
*.map
node_modules/**
.vscode/**
.planning/**
docs/**
examples/**
```

**.vscode/launch.json** — Per PATTERNS.md:
- Two configurations: "Run Extension" (extensionHost, outFiles in dist/) and "Extension Tests" (extensionHost, outFiles in out/test/)
- preLaunchTask: `${defaultBuildTask}` for Run Extension, `npm: compile` for tests

**media/icon.svg** — Per PATTERNS.md, minimal placeholder:
```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16">
  <rect width="16" height="16" fill="#007ACC"/>
</svg>
```

Create directories `media/` and `.vscode/` before writing files.
  </action>
  <verify>
    <automated>test -f package.json && test -f tsconfig.json && test -f esbuild.js && test -f .vscode-test.mjs && test -f .vscodeignore && test -f .vscode/launch.json && test -f media/icon.svg && echo "All config files exist"</automated>
  </verify>
  <acceptance_criteria>
- package.json contains `"engines": {"vscode": "^1.90.0"}`
- package.json contains `"main": "./dist/extension.js"`
- package.json contains `"activationEvents": []`
- package.json contains `"logautopsy.runAnalysis"`
- package.json contains `"logautopsy-container"`
- package.json contains `"logautopsy.sidebar"`
- package.json contains `"npm-run-all"`
- package.json contains `"typescript": "~5.4"`
- package.json contains `"esbuild": "^0.21"`
- package.json contains `"vscode-test"` in test script
- tsconfig.json contains `"strict": true`
- tsconfig.json contains `"outDir": "out"`
- tsconfig.json contains `"target": "ES2024"`
- tsconfig.json contains `"module": "commonjs"`
- esbuild.js contains `external: ['vscode']`
- esbuild.js contains `format: 'cjs'`
- esbuild.js contains `src/extension/activate.ts`
- esbuild.js contains `dist/extension.js`
- .vscode-test.mjs contains `defineConfig`
- .vscode-test.mjs contains `out/test/**/*.test.js`
- .vscode/launch.json contains `extensionHost`
- media/icon.svg contains `svg`
  </acceptance_criteria>
  <done>All 7 configuration files exist with correct content matching PATTERNS.md specifications</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and verify build toolchain</name>
  <files>package-lock.json, .gitignore</files>
  <read_first>
    package.json
    .gitignore
  </read_first>
  <action>
1. Run `npm install` in the project root to install all devDependencies declared in package.json.

2. Update `.gitignore` to include the following entries (add to existing .gitignore if present, or create if missing):
```
node_modules/
dist/
out/
.vscode-test/
*.vsix
```

3. Verify the toolchain is accessible:
   - Run `npx tsc --version` — should print a 5.4.x version
   - Run `npx esbuild --version` — should print 0.21.x or higher
   - Run `npx vscode-test --help` — should print usage (not "command not found")

Note: `npm run compile` will fail at this point because source files do not exist yet. That is expected. This task only verifies that dependencies installed correctly and CLI tools are available.
  </action>
  <verify>
    <automated>cd /Users/pavelspakowski/sw_dev/LogAutopsy && test -d node_modules && npx tsc --version && npx esbuild --version && echo "Toolchain verified"</automated>
  </verify>
  <acceptance_criteria>
- node_modules/ directory exists
- package-lock.json exists
- .gitignore contains `node_modules/`
- .gitignore contains `dist/`
- .gitignore contains `out/`
- .gitignore contains `.vscode-test/`
- `npx tsc --version` outputs a version starting with 5.4
- `npx esbuild --version` outputs a version starting with 0.2
  </acceptance_criteria>
  <done>All devDependencies installed, CLI tools accessible, .gitignore updated with build output directories</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build tooling only | No user input, no network calls, no extension host runtime in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm dependencies | accept | Standard npm install from registry; package-lock.json pins versions; no production secrets in this phase |
| T-01-02 | Information Disclosure | .vscodeignore | mitigate | .vscodeignore excludes src/, test/, .planning/, docs/ from VSIX package — prevents shipping source and planning artifacts |
</threat_model>

<verification>
- All 7 config files exist at expected paths
- npm install completes without errors
- .gitignore excludes build outputs
- CLI tools (tsc, esbuild, vscode-test) are accessible via npx
</verification>

<success_criteria>
- package.json is a valid VS Code extension manifest with all contribution points
- tsconfig.json, esbuild.js, .vscode-test.mjs are correctly configured
- node_modules/ contains all required devDependencies
- Build toolchain CLIs respond to version/help queries
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md`
</output>
